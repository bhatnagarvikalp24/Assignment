Revenue Generated

SELECT
  app_id,
  SUM(total_revenue_in_paise) / 100 AS total_revenue
FROM
  rewards_details
GROUP BY
  app_id;

This query calculates the total revenue generated by each app based on the rewards offered. It helps determine the financial performance of each app.
